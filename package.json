{
  "name": "tabsircg-workspace",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter admin dev",
    "dev:portfolio": "pnpm --filter portfolio dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "tc": "pnpm -r tc"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp", "msw", "protobufjs", "unrs-resolver"]
  }
}
