{
  "name": "@tabsircg/analytics-mcp",
  "version": "0.3.0",
  "description": "MCP server for instrumenting a site with @tabsircg/analytics",
  "type": "module",
  "bin": {
    "tabsircg-analytics-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "test": "vitest run",
    "tc": "tsc --noEmit"
  },
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tabsircg/schemas": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.10"
  }
}
